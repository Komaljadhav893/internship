#yuklidien approch
def gcd(a,b):
    if a==0:
        return  b 
    if b==0:
        return a
    if a==b:
        return a 
    if a>b:
        return gcd(a-b,b)
    else:
        return gcd(b-a,a)
        
a=int(input())        
b=int(input())        #time com : min(a,b)
print(gcd(a,b))

def gcd(a,b):
    if b==0:
        return a
    
    return gcd(b,a%b)
        
a=int(input())        
b=int(input())        
print(gcd(a,b))
