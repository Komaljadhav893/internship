class Solution:
    def removeStars(self, s: str) -> str:
        st=[]
        for i in s:
            if i=="*":
                st.pop()
                # st.pop(1)
                continue
            st.append(i)    
        return "".join(st)   
        
